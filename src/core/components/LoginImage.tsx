function LoginImage() {
    return (
        <div className="login-image h-100 d-flex flex-column justify-content-evenly p-3">
            <h4 className="text-light p-5">"Operations keeps the lights on, strategy provides a light at the end of the tunnel,
                but project management
                is the train engine that moves the organization forward."
            </h4>
            <h2 className="text-light text-end">
                - Joy Gumz
            </h2>
        </div>
    )
};

export default LoginImage;
